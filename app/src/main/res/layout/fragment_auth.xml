<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/authBackground"
    android:gravity="center" >

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp" >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:id="@+id/auth_text"
            android:textSize="@dimen/text_auth"
            android:textColor="@color/colorText"
            android:gravity="center" />
    </LinearLayout>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <Button style="@style/SocialButton"
            android:text="@string/vk_btn_text"
            android:id="@+id/vk"
            android:background="@drawable/vk_social_button"
            android:drawableStart="@drawable/vk" />

        <Button style="@style/SocialButton"
            android:text="@string/gp_btn_text"
            android:id="@+id/gp"
            android:background="@drawable/gp_social_button"
            android:drawableStart="@drawable/gp" />
    </LinearLayout>

</LinearLayout>